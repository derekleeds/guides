{
  "name": "guides.derekleeds.cloud",
  "version": "1.0.0",
  "description": "How-to guides for AI agents, homelab, and self-hosting",
  "scripts": {
    "get:submodules": "git submodule update --init --recursive",
    "build": "hugo --gc --minify",
    "serve": "hugo server --bind 0.0.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.31",
    "postcss-cli": "^10.1.0"
  }
}
